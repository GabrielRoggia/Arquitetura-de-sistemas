// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// A tabela que o Prisma irá criar
model clients {
  id         String   @id @default(uuid())
  name       String
  email      String   @unique
  password   String
  is_deleted Boolean  @default(false)
}